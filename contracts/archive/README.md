# Archived Contracts — Stale Syntax Hazard

> **WARNING**: This contract uses OLD Compact syntax from 2025.
> It does NOT compile on `compactc 0.31.1` and is kept for historical reference only.

## Do NOT use this as a code example

The active registry contracts are in the parent directory:
- `../DIDzRegistry.compact` — compiles on 0.31.1
- `../TrustedIssuerRegistry.compact` — compiles on 0.31.1

## What's here

| File | Era | Why archived |
|------|-----|--------------|
| `DIDzRegistry_v1_mvp.compact` | 2025 MVP | Pre-engine, pre-privacy-first rework |
